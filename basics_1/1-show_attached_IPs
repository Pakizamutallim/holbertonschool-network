#!/usr/bin/env bash
# This script displays all active IPv4 IPs on the machine

# Using `ip` command to list all IP addresses associated with the network interfaces
ip -4 addr show | grep -oP '(?<=inet\s)\d+(\.\d+){3}'

# Alternative using `ifconfig` (if available)
# ifconfig | grep -oP 'inet \K\d+(\.\d+){3}'
